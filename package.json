{
  "name": "sketchify",
  "version": "1.0.0",
  "description": "Native Rust sketching app on musl",
  "main": "main.js",
  "scripts": {
    "start": "electron35 . --ozone-platform-hint=auto --enable-features=WaylandWindowDecorations",
    "build": "RUSTFLAGS='-C target-feature=-crt-static' RUST_MIN_STACK=8388608 cargo build --release && cp target/release/libsketchify_native.so ./index.node"
  },
  "keywords": [
    "Electron",
    "Rust",
    "Neon",
    "musl"
  ],
  "author": "Quasar",
  "license": "MIT",
  "devDependencies": {
    "@neon-rs/cli": "^1.0.0"
  },
  "dependencies": {
    "image": "^0.24.0"
  }
}
